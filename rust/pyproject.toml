[build-system]
requires = ["maturin>=1.15,<2.0"]
build-backend = "maturin"

[project]
name = "hermes-rust"
version = "0.1.0"
requires-python = ">=3.11"

[tool.maturin]
bindings = "pyo3"
module-name = "hermes._rust"
python-source = "../"